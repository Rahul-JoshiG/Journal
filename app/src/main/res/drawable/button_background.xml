<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_pressed="true">
        <shape>
            <!-- Pressed state color -->
            <solid android:color="@color/brown" /> <!-- Darker purple when pressed -->
            <corners android:radius="20dp" />
        </shape>
    </item>

    <item>
        <shape>
            <!-- Default state color -->
            <solid android:color="@color/brown" /> <!-- Default purple background -->
            <corners android:radius="20dp" />
        </shape>
    </item>
</selector>
